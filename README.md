# Use this project to set up a newly installed Ubuntu 64bit .deb

In order to run the setup:
```
sudo apt install wget
wget -O - https://raw.githubusercontent.com/kdkocev/pimpmydrive/master/setup.sh | bash
```